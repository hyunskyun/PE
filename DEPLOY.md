# 배포 방법 3가지

핵심 로직(랜드마크 추출 → 각도/거리 계산 → 구간 분류 → 임계값 판정 → 피드백 출력)은
`posture_analysis/` 파이썬 패키지 한 곳에 있고, 아래 세 가지 형태 중 어떤 것으로도
같은 로직을 노출할 수 있다.

## 1. 데스크톱 프로그램 (파이썬)

```bash
pip install -r requirements.txt
python main.py --video sprint.mp4 --height 170
python main.py --video sprint.mp4 --height 170 --split-time 2.5 --output report.json
```

- `--height`: 사용자 신장(cm). 없어도 비율 계산 자체는 가능하지만, 있으면 리포트에서
  실제 신장 대비 보폭 등을 해석하기 쉬워진다.
- `--split-time`: 50m 구간 중 가속 구간이 끝나는 시각(초). 생략하면 영상 앞부분 35%를
  자동으로 가속 구간으로 본다.
- 첫 실행 시 포즈 인식 모델(`pose_landmarker_lite.task`, 약 6MB)을 자동으로 내려받는다.

## 2. Cloudflare Pages 정적 사이트

`web/` 폴더는 순수 정적 파일(HTML/CSS/JS)이며, MediaPipe Pose Landmarker를
브라우저(WASM)에서 직접 실행하므로 백엔드 서버가 필요 없다. 영상도 사용자 브라우저를
벗어나지 않는다.

```bash
# Cloudflare 대시보드에서 web/ 폴더를 그대로 드래그 앤 드롭하거나,
npx wrangler pages deploy web --project-name posture-analysis
```

- `web/app.js` 상단의 `THRESHOLDS`, `FEEDBACK_TEMPLATES` 는 파이썬 `posture_analysis/config.py`
  와 같은 값으로 맞춰져 있다. 판정 기준을 바꿀 때는 두 파일을 함께 수정해야 한다.
- CDN 경로(`@mediapipe/tasks-vision@0.10.14`)가 만료되면 최신 버전 번호로 교체한다.

## 3. Google AI Studio로 안드로이드 앱 만들기

[Google AI Studio](https://aistudio.google.com)의 앱 빌드 기능에 아래 스펙을
그대로 붙여넣으면, 같은 계산 로직을 가진 안드로이드/웹 앱 초안을 생성할 수 있다.
숫자(임계값)와 랜드마크 인덱스는 `posture_analysis/config.py` 값을 그대로 옮긴 것이므로,
이후 값을 바꾸면 이 프롬프트도 함께 갱신한다.

```
MediaPipe Pose를 사용해 50m 달리기 측면 영상을 분석하는 앱을 만들어줘.

[핵심 랜드마크] 어깨(11,12), 팔꿈치(13,14), 손목(15,16), 엉덩이(23,24), 무릎(25,26), 발목(27,28)

[계산 지표]
- 상체 전방 기울기: 엉덩이 중심→어깨 중심 벡터가 수직선과 이루는 각도(진행 방향 쪽으로 기운 정도)
- 무릎 굴곡각: 엉덩이-무릎-발목 사잇각
- 보폭 비율: 양 발목 사이 거리 / (코~발목 중점 세로 거리로 추정한 신장 픽셀값)
- 착지 시 발목-엉덩이 수평 거리 비율: 착지 순간 발목 x좌표와 엉덩이 중심 x좌표 차이 / 신장 픽셀값

[구간 분류] 영상 앞부분 35%(또는 사용자가 입력한 시간)를 "가속 구간", 나머지를 "최대 속도 구간"으로 분류

[판정 기준]
가속 구간: 상체 기울기 15~45도, 보폭 비율 0.80~1.15, 무릎 굴곡각 100~165도, 착지 수평거리 비율 0~0.12
최대 속도 구간: 상체 기울기 5~12도, 보폭 비율 0.90~1.25, 무릎 굴곡각 110~170도, 착지 수평거리 비율 0~0.08

[출력] 값이 범위를 벗어나면 오류 유형과 한국어 교정 문장을 출력하고,
구간별 평균값, 오류 발생 횟수, 우선 교정이 필요한 항목, 0~100점 점수를 함께 보여줘.
```

Google AI Studio가 생성한 코드에서 계산식/임계값 부분만 위 스펙과 대조해 확인하면
`posture_analysis` 파이썬 코드와 같은 방식으로 동작하는지 검증할 수 있다.
